case_id: "resp-redact-passwd"
title: "Redact /etc/passwd content in response"
action_envelope:
  action_type: "MCP_TOOL_CALL"
  actor: "agent:test"
  target: "read_file"
  parameters:
    path: "/etc/passwd"
  request_id: "req-007"
response_sample:
  raw:
    content:
      - type: "text"
        text: "root:x:0:0:root:/root:/bin/bash\n.../etc/passwd..."
expectations:
  policies:
    - policy_id: "redact-passwd-content"
      decision: "redact"
      reason_tags: ["system-file"]
  overall:
    decision: "redact"
    min_confidence: 0.9
notes:
  - "CEL response rule should redact passwd content."
