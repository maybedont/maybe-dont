case_id: "req-cli-command-exec-deny"
title: "Deny dangerous command execution tools"
action_envelope:
  action_type: "CLI_EXEC"
  actor: "agent:test"
  target: "bash"
  parameters:
    command: "ls -la"
  estimated_impact:
    scope: "system"
    data_classification: ["system"]
  request_id: "req-008"
expectations:
  policies:
    - policy_id: "Check command execution tools"
      decision: "deny"
      reason_tags: ["dangerous-tool"]
  overall:
    decision: "deny"
    min_confidence: 0.8
notes:
  - "Command execution tools should be blocked by default AI rule."
