case_id: "resp-redact-ssn"
title: "Redact SSN in response"
action_envelope:
  action_type: "MCP_TOOL_CALL"
  actor: "agent:test"
  target: "db__read_customer"
  parameters:
    id: "cust_123"
  request_id: "req-005"
response_sample:
  raw:
    customer:
      name: "Jane Doe"
      ssn: "123-45-6789"
expectations:
  policies:
    - policy_id: "detect-pii-in-response"
      decision: "redact"
      redact_fields: ["customer.ssn"]
  overall:
    decision: "redact"
    min_confidence: 0.7
notes:
  - "PII should be redacted before returning to agent."
