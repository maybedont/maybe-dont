---
name: Bug report
about: Report a problem with the gateway
title: ''
labels: bug
assignees: ''
---

**Describe the bug**
A clear description of what went wrong and what you expected instead.

**Version**
Output of `maybe-dont version`:

```
```

**Configuration**
Relevant parts of `maybe-dont.yaml` and any CEL/AI rules files, with secrets
(API keys, tokens, `${VAR}` values) redacted.

```yaml
```

**Steps to reproduce**
1.
2.
3.

**Audit log excerpt**
If applicable, the relevant entries from the audit log
(`maybedont-audit.log` by default), redacted as needed.

```json
```

**Additional context**
Anything else relevant — OS/platform, downstream MCP server(s) involved,
whether this happens with `mode: enforce` or `audit_only`, etc.
